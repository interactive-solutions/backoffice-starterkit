
import { AuthenticationStorage } from './storage';
import { AuthenticationService } from './service';

export const authenticationStorage: AuthenticationStorage = new AuthenticationStorage();
export const authenticationService: AuthenticationService = new AuthenticationService();
